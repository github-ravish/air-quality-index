build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: 
    command:
      - gunicorn gunicorn django-app.wsgi:application --bind 0.0.0.0:$PORT
    image: web
  worker: 
    command:
      - ./celeryconfig.sh
    image: worker